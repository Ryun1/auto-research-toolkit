# Q1 — fold is free ops

Measured at unroll=1, width=32, cache=0: objective 34,133,994 -> 29,055,448,
a fall of 14.88%. `peak` is unchanged at 34, so the whole move is in `ops`.
Pre-registered bar was ">10% confirms". Confirmed.
